# Production Profile Configuration

# Database Configuration - MySQL Example (commented)
# spring.datasource.url=jdbc:mysql://localhost:3306/copilot_prompter_prod
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# MongoDB Configuration Example (commented)
# spring.data.mongodb.uri=${MONGODB_URI}
# spring.data.mongodb.database=copilot_prompter_prod

# Security Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Actuator Configuration
management.endpoints.web.exposure.include=health,info

# Logging Configuration
logging.level.com.copilotprompter=INFO
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
