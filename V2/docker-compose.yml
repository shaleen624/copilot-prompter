# docker-compose up --build
# This will build the image and serve your Angular app at http://localhost:8080
version: '3.8'
services:
  copilot-prompter:
    build: .
    image: copilot-prompter:latest
    ports:
      - "8080:80"
    restart: unless-stopped
